import 'dotenv/config';import {startWorker} from '../lib/queue';import {executePipeline} from '../lib/pipeline/steps';const worker=startWorker(async job=>{const {projectId,transcript,steps}=job.data;await job.updateProgress(10);const result=await executePipeline({projectId,transcript},steps);await job.updateProgress(100);return result});worker.on('completed',j=>console.log(`completed ${j.id}`));worker.on('failed',(j,e)=>console.error(`failed ${j?.id}`,e));console.log('ClipForge worker listening');
