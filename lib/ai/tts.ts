export async function synthesize(text:string,voice='alloy'){if(!process.env.OPENAI_API_KEY)throw new Error('OPENAI_API_KEY is required for TTS');const r=await fetch('https://api.openai.com/v1/audio/speech',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_TTS_MODEL||'gpt-4o-mini-tts',voice,input: text,response_format:'mp3'})});if(!r.ok)throw new Error(`TTS failed ${r.status}`);return Buffer.from(await r.arrayBuffer())}
