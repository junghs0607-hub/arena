export type UploadOptions={accessToken:string;file:Blob;title:string;description:string;tags:string[];privacy:'private'|'unlisted'|'public';publishAt?:string};
export async function uploadVideo(o:UploadOptions){const metadata={snippet:{title:o.title,description:o.description,tags:o.tags},status:{privacyStatus:o.privacy,...(o.publishAt?{publishAt:o.publishAt}: {})}};const init=await fetch('https://www.googleapis.com/upload/youtube/v3/videos?part=snippet,status&uploadType=resumable',{method:'POST',headers:{Authorization:`Bearer ${o.accessToken}`,'Content-Type':'application/json','X-Upload-Content-Type':o.file.type||'video/mp4','X-Upload-Content-Length':String(o.file.size)},body:JSON.stringify(metadata)});if(!init.ok)throw new Error(`youtube init failed ${init.status}`);const location=init.headers.get('location');if(!location)throw new Error('youtube upload location missing');const result=await fetch(location,{method:'PUT',headers:{Authorization:`Bearer ${o.accessToken}`,'Content-Type':o.file.type||'video/mp4','Content-Length':String(o.file.size)},body:o.file});if(!result.ok)throw new Error(`youtube upload failed ${result.status}`);return result.json()}
