export async function fetchVideoStatistics(accessToken:string,videoId:string){const r=await fetch(`https://www.googleapis.com/youtube/v3/videos?part=statistics&id=${encodeURIComponent(videoId)}`,{headers:{Authorization:`Bearer ${accessToken}`}});if(!r.ok)throw new Error(`youtube statistics failed ${r.status}`);const j=await r.json();const s=j.items?.[0]?.statistics||{};return {views:Number(s.viewCount||0),likes:Number(s.likeCount||0),comments:Number(s.commentCount||0)}}
