import {decrypt,encrypt} from '../security';export async function refreshYoutubeToken(refreshToken:string){const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({client_id:process.env.YOUTUBE_CLIENT_ID||'',client_secret:process.env.YOUTUBE_CLIENT_SECRET||'',refresh_token:decrypt(refreshToken),grant_type:'refresh_token'})});if(!r.ok)throw new Error(`refresh failed ${r.status}`);const t=await r.json();return {accessToken:encrypt(t.access_token),expiresAt:new Date(Date.now()+(t.expires_in||3600)*1000)}}
