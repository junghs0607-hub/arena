export async function sendEmail(to:string,subject:string,html:string){if(!process.env.RESEND_API_KEY){console.log(`[email:dev] ${to} ${subject}\n${html}`);return {id:'dev'}}const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.EMAIL_FROM||'ClipForge <onboarding@resend.dev>',to:[to],subject,html})});if(!r.ok)throw new Error(`email failed ${r.status}`);return r.json()}
