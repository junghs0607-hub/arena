import {writeFile} from 'node:fs/promises';export async function writeVtt(file:string,segments:{start:number;end:number;text:string}[]){const time=(n:number)=>{const h=Math.floor(n/3600).toString().padStart(2,'0');const m=Math.floor(n%3600/60).toString().padStart(2,'0');const s=(n%60).toFixed(3).padStart(6,'0');return `${h}:${m}:${s}`};await writeFile(file,['WEBVTT','',...segments.map(s=>`${time(s.start)} --> ${time(s.end)}\n${s.text}\n`)].join('\n'));return file}
