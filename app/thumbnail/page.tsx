'use client';import {useState} from 'react';export default function Thumbnail(){const [projectId,setProject]=useState(''),[file,setFile]=useState<File|null>(null),[url,setUrl]=useState(''),[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();if(!file)return setMsg('영상 파일을 선택하세요.');const fd=new FormData();fd.append('file',file);fd.append('projectId',projectId);setMsg('썸네일 생성 중...');const r=await fetch('/api/thumbnail',{method:'POST',body:fd});if(!r.ok)return setMsg('생성 실패');setUrl(URL.createObjectURL(await r.blob()));setMsg('썸네일 생성 완료')}return <main style={{maxWidth:760,margin:'40px auto',padding:24}}><h1>썸네일</h1><p>영상 프레임에서 YouTube 썸네일을 생성합니다.</p><form onSubmit={go} style={{background:'#fff',padding:20,borderRadius:12,display:'grid',gap:10}}><input required placeholder="Project ID" value={projectId} onChange={e=>setProject(e.target.value)}/><input required type="file" accept="video/*" onChange={e=>setFile(e.target.files?.[0]||null)}/><button className="primary">썸네일 생성</button></form>{url&&<><img src={url} alt="generated thumbnail" style={{width:'100%',marginTop:20,borderRadius:10}}/><a className="primary" href={url} download="thumbnail.jpg">다운로드</a></>}<p>{msg}</p></main>}
