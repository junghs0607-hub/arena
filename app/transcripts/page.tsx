'use client';import {useState} from 'react';export default function Transcripts(){const [sourceId,setSource]=useState(''),[id,setId]=useState(''),[text,setText]=useState(''),[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/transcripts?sourceId='+sourceId);if(r.ok){const a=await r.json();const t=a[0];if(t){setId(t.id);setText(t.text)}}else setMsg('소스 ID 또는 로그인 상태를 확인해주세요.')}async function save(){const r=await fetch('/api/transcripts',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,text})});setMsg(r.ok?'Transcript가 저장되었습니다.':'저장 실패')}return <main style={{maxWidth:900,margin:'40px auto',padding:24}}><h1>Transcript 편집</h1><p>STT 결과를 검토하고 수정합니다.</p><div style={{background:'#fff',padding:20,borderRadius:12}}><input placeholder="Source ID" value={sourceId} onChange={e=>setSource(e.target.value)}/><button className="primary" onClick={load}>불러오기</button><textarea rows={16} placeholder="Transcript" value={text} onChange={e=>setText(e.target.value)}/><button className="primary" disabled={!id} onClick={save}>저장</button><p>{msg}</p></div></main>}
