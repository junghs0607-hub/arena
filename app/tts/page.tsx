'use client';import {useState} from 'react';export default function Tts(){const [projectId,setProject]=useState(''),[text,setText]=useState(''),[voice,setVoice]=useState('alloy'),[audio,setAudio]=useState(''),[msg,setMsg]=useState('');async function create(e:React.FormEvent){e.preventDefault();setMsg('생성 중...');const r=await fetch('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId,text,voice})});if(!r.ok){setMsg('생성 실패');return}const b=await r.json();setAudio('/api/files?key='+encodeURIComponent(b.key));setMsg('음성이 생성되었습니다.')}return <main style={{maxWidth:760,margin:'40px auto',padding:24}}><h1>음성 / TTS</h1><p>대본을 음성으로 변환합니다.</p><form onSubmit={create} style={{background:'#fff',padding:20,borderRadius:12,display:'grid',gap:10}}><input required placeholder="Project ID" value={projectId} onChange={e=>setProject(e.target.value)}/><select value={voice} onChange={e=>setVoice(e.target.value)}><option>alloy</option><option>ash</option><option>coral</option><option>sage</option></select><textarea required rows={10} placeholder="읽을 대본" value={text} onChange={e=>setText(e.target.value)}/><button className="primary">음성 생성</button></form>{audio&&<audio controls src={audio} style={{width:'100%',marginTop:20}}/>}<p>{msg}</p></main>}
