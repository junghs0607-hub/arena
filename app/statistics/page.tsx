'use client';import {useEffect,useState} from 'react';export default function Statistics(){const [data,setData]=useState<any>({views:0,likes:0,comments:0}),[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/statistics');if(r.ok)setData(await r.json());else setMsg('로그인 후 이용하세요.')}useEffect(()=>{load()},[]);async function sync(){setMsg('동기화 중...');const r=await fetch('/api/statistics/sync',{method:'POST'});setMsg(r.ok?'동기화 완료':'동기화 실패');load()}return <main style={{maxWidth:900,margin:'40px auto',padding:24}}><h1>YouTube 통계</h1><p>내 채널 업로드 성과를 확인합니다.</p><button className="primary" onClick={sync}>통계 새로고침</button><section style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:12,marginTop:20}}>{[['조회수',data.views],['좋아요',data.likes],['댓글',data.comments]].map(x=><article key={String(x[0])} style={{background:'#fff',padding:24,borderRadius:12}}><small>{x[0]}</small><h2>{Number(x[1]).toLocaleString()}</h2></article>)}</section><p>{msg}</p></main>}
