import {NextResponse} from 'next/server';import {currentUser} from '../../../../lib/auth';import {db} from '../../../../lib/db';import {youtubeChannels} from '../../../../drizzle/schema';import {eq} from 'drizzle-orm';export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});try{return NextResponse.json(await db.select({id:youtubeChannels.id,channelId:youtubeChannels.channelId,title:youtubeChannels.title,expiresAt:youtubeChannels.expiresAt}).from(youtubeChannels).where(eq(youtubeChannels.userId,u.id)))}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
