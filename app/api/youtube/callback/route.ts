import {NextResponse} from 'next/server';
export async function GET(req:Request){const code=new URL(req.url).searchParams.get('code');if(!code)return NextResponse.json({error:'OAUTH_CODE_REQUIRED'},{status:400});if(!process.env.YOUTUBE_CLIENT_ID||!process.env.YOUTUBE_CLIENT_SECRET)return NextResponse.json({error:'YOUTUBE_NOT_CONFIGURED'},{status:503});const redirect=process.env.YOUTUBE_REDIRECT_URI||'http://localhost:3000/api/youtube/callback';const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({code,client_id:process.env.YOUTUBE_CLIENT_ID,client_secret:process.env.YOUTUBE_CLIENT_SECRET,redirect_uri:redirect,grant_type:'authorization_code'})});if(!r.ok)return NextResponse.json({error:'TOKEN_EXCHANGE_FAILED'},{status:502});const tokens=await r.json();return NextResponse.json({ok:true,message:'Tokens received. Persist encrypted tokens for the authenticated user.',expires_in:tokens.expires_in})}
