import {randomBytes} from 'node:crypto';import {cookies} from 'next/headers';import {NextResponse } from 'next/server';
export async function GET(){const id=process.env.YOUTUBE_CLIENT_ID;if(!id)return NextResponse.json({error:'YOUTUBE_NOT_CONFIGURED'},{status:503});const state=randomBytes(24).toString('hex');(await cookies()).set('youtube_oauth_state',state,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:600,path:'/'});const redirect=process.env.YOUTUBE_REDIRECT_URI||'http://localhost:3000/api/youtube/callback';const params=new URLSearchParams({client_id:id,redirect_uri:redirect,response_type:'code',access_type:'offline',scope:'https://www.googleapis.com/auth/youtube.upload https://www.googleapis.com/auth/youtube.readonly',prompt:'consent',state});return NextResponse.redirect('https://accounts.google.com/o/oauth2/v2/auth?'+params)}
