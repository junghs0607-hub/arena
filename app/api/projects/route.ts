import {NextResponse} from 'next/server';import {videoProjects,automationJobs} from '../../../drizzle/schema';import {db} from '../../../lib/db';import {projectInput} from '../../../lib/validation';
export async function GET(){try{return NextResponse.json(await db.select().from(videoProjects))}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
export async function POST(req:Request){const parsed=projectInput.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'VALIDATION_ERROR',details:parsed.error.flatten()},{status:400});try{const [p]=await db.insert(videoProjects).values({...parsed.data,status:'queued'}).returning();await db.insert(automationJobs).values({projectId:p.id,type:'analyze_source',payload:{pipeline:['analyze_source','stt','script','tts','subtitles','render','thumbnail','metadata']}});return NextResponse.json(p,{status:201})}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
