import {currentUser} from '../../../lib/auth';import {NextResponse} from 'next/server';import {executePipeline} from '../../../lib/pipeline/steps';import {configuredProvider} from '../../../lib/ai/providers';
export async function POST(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const b=await req.json();if(!b.projectId)return NextResponse.json({error:'PROJECT_ID_REQUIRED'},{status:400});try{const result=await executePipeline({projectId:b.projectId,transcript:b.transcript||''},b.steps||['analyze_source','script','metadata']);return NextResponse.json({ok:true,provider:configuredProvider().name,result})}catch(e){return NextResponse.json({error:'PIPELINE_FAILED',message:String(e)},{status:500})}}
