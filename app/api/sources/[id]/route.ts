import {NextResponse} from 'next/server';import {videoSources} from '../../../../drizzle/schema';import {db} from '../../../../lib/db';import {currentUser} from '../../../../lib/auth';import {and,eq} from 'drizzle-orm';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const {id}=await params;const r=await db.select().from(videoSources).where(and(eq(videoSources.id,id),eq(videoSources.userId,u.id)));return r[0]?NextResponse.json(r[0]):NextResponse.json({error:'NOT_FOUND'},{status:404})}export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const {id}=await params;const b=await req.json();const r=await db.update(videoSources).set({...(b.title?{title:b.title}:{}),...(b.licenseNote?{licenseNote:b.licenseNote}:{}),...(typeof b.licenseConfirmed==='boolean'?{licenseConfirmed:b.licenseConfirmed}:{})}).where(and(eq(videoSources.id,id),eq(videoSources.userId,u.id))).returning();return r[0]?NextResponse.json(r[0]):NextResponse.json({error:'NOT_FOUND'},{status:404})}export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const {id}=await params;const r=await db.delete(videoSources).where(and(eq(videoSources.id,id),eq(videoSources.userId,u.id))).returning({id:videoSources.id});return r[0]?new NextResponse(null,{status:204}):NextResponse.json({error:'NOT_FOUND'},{status:404})}
