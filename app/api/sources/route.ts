import {NextResponse} from 'next/server';import {videoSources} from '../../../drizzle/schema';import {db} from '../../../lib/db';import {sourceInput} from '../../../lib/validation';
export async function GET(){try{return NextResponse.json(await db.select().from(videoSources))}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
export async function POST(req:Request){const parsed=sourceInput.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'VALIDATION_ERROR',details:parsed.error.flatten()},{status:400});try{const [source]=await db.insert(videoSources).values({...parsed.data,status:'pending'}).returning();return NextResponse.json(source,{status:201})}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
