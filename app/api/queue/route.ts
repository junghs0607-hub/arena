import {NextResponse} from 'next/server';import {pipelineQueue} from '../../../lib/queue';export async function POST(req:Request){const b=await req.json();if(!b.projectId)return NextResponse.json({error:'PROJECT_ID_REQUIRED'},{status:400});const job=await pipelineQueue.add('pipeline',{projectId:b.projectId,transcript:b.transcript||'',steps:b.steps||['analyze_source','script','metadata']},{attempts:3,backoff:{type:'exponential',delay:2000},removeOnComplete:100,removeOnFail:100});return NextResponse.json({ok:true,jobId:job.id},{status:202})}
