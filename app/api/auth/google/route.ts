import {NextResponse} from 'next/server';export async function GET(){const id=process.env.GOOGLE_CLIENT_ID;if(!id)return NextResponse.json({error:'GOOGLE_NOT_CONFIGURED'},{status:503});const redirect=process.env.GOOGLE_REDIRECT_URI||'http://localhost:3000/api/auth/google/callback';const q=new URLSearchParams({client_id:id,redirect_uri:redirect,response_type:'code',scope:'openid email profile',access_type:'offline'});return NextResponse.redirect('https://accounts.google.com/o/oauth2/v2/auth?'+q)}
