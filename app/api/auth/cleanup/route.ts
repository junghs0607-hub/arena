import {NextResponse} from 'next/server';import {db} from '../../../../lib/db';import {sessions} from '../../../../drizzle/schema';import {lt} from 'drizzle-orm';export async function POST(req:Request){if(process.env.CLEANUP_SECRET&&req.headers.get('x-cleanup-secret')!==process.env.CLEANUP_SECRET)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});await db.delete(sessions).where(lt(sessions.expiresAt,new Date()));return NextResponse.json({ok:true})}
