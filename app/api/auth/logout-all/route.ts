import {NextResponse} from 'next/server';import {db} from '../../../../lib/db';import {sessions} from '../../../../drizzle/schema';import {currentUser,clearSession} from '../../../../lib/auth';import {eq} from 'drizzle-orm';export async function POST(){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});await db.delete(sessions).where(eq(sessions.userId,u.id));await clearSession();return NextResponse.json({ok:true})}
