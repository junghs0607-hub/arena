import {NextResponse} from 'next/server';import {automationJobs} from '../../../drizzle/schema';import {db} from '../../../lib/db';import {desc,eq} from 'drizzle-orm';import {currentUser} from '../../../lib/auth';export async function GET(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const projectId=new URL(req.url).searchParams.get('projectId');try{return NextResponse.json(await db.select().from(automationJobs).innerJoin((await import('../../../drizzle/schema')).videoProjects,eq(automationJobs.projectId,(await import('../../../drizzle/schema')).videoProjects.id)).where(projectId?eq(automationJobs.projectId,projectId):eq((await import('../../../drizzle/schema')).videoProjects.userId,u.id)).orderBy(desc(automationJobs.createdAt)))}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
