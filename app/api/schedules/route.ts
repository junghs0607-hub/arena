import {NextResponse} from 'next/server';import {automationSchedules} from '../../../drizzle/schema';import {db} from '../../../lib/db';import {z} from 'zod';import {desc} from 'drizzle-orm';const input=z.object({projectId:z.string().uuid(),cron:z.string().min(5).max(100),timezone:z.string().default('Asia/Seoul'),enabled:z.boolean().default(true)});
export async function GET(){try{return NextResponse.json(await db.select().from(automationSchedules).orderBy(desc(automationSchedules.nextRunAt)))}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
export async function POST(req:Request){const p=input.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'VALIDATION_ERROR',details:p.error.flatten()},{status:400});try{const [s]=await db.insert(automationSchedules).values(p.data).returning();return NextResponse.json(s,{status:201})}catch{return NextResponse.json({error:'DATABASE_UNAVAILABLE'},{status:503})}}
