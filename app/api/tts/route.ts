import {ownsProject} from '../../../lib/ownership';import {currentUser} from '../../../lib/auth';import {NextResponse} from 'next/server';import {synthesize} from '../../../lib/ai/tts';export const runtime='nodejs';export async function POST(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const b=await req.json();if(!b.projectId||!await ownsProject(u.id,b.projectId))return NextResponse.json({error:'NOT_FOUND'},{status:404});if(!b.text)return NextResponse.json({error:'TEXT_REQUIRED'},{status:400});try{const audio=await synthesize(b.text,b.voice);return new NextResponse(audio,{headers:{'Content-Type':'audio/mpeg','Content-Disposition':'attachment; filename="voice.mp3"'}})}catch(e){return NextResponse.json({error:'TTS_FAILED',message:String(e)},{status:500})}}
