'use client';import {useState} from 'react';export default function Metadata(){const [projectId,setProject]=useState(''),[script,setScript]=useState(''),[data,setData]=useState<any>(null),[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/ai/metadata',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId,script})});if(r.ok)setData(await r.json());else setMsg('프로젝트 권한 또는 설정을 확인해주세요.')}return <main style={{maxWidth:800,margin:'40px auto',padding:24}}><h1>YouTube Metadata</h1><p>AI로 제목, 설명, 태그를 생성합니다.</p><form onSubmit={go} style={{background:'#fff',padding:20,borderRadius:12,display:'grid',gap:10}}><input required placeholder="Project ID" value={projectId} onChange={e=>setProject(e.target.value)}/><textarea required rows={10} placeholder="대본" value={script} onChange={e=>setScript(e.target.value)}/><button className="primary">Metadata 생성</button></form>{data&&<article style={{background:'#fff',padding:20,marginTop:20,borderRadius:12}}><h2>제목 후보</h2><ul>{data.titles?.map((x:string)=><li key={x}>{x}</li>)}</ul><h2>설명</h2><p>{data.description}</p><h2>태그</h2><p>{data.tags?.join(', ')}</p></article>}<p>{msg}</p></main>}
