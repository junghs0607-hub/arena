'use client';import {useEffect,useState} from 'react';export default function Reset(){const [token,setToken]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');useEffect(()=>setToken(new URLSearchParams(window.location.search).get('token')||''),[]);async function go(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/reset/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});setMsg(r.ok?'비밀번호가 변경되었습니다.':'토큰이 만료되었거나 올바르지 않습니다.')}return <main style={{maxWidth:420,margin:'100px auto',padding:32,background:'#fff',borderRadius:16}}><h1>비밀번호 재설정</h1><p>새 비밀번호를 설정하세요.</p><form onSubmit={go}><input required placeholder="재설정 토큰" value={token} onChange={e=>setToken(e.target.value)}/><input required minLength={8} type="password" placeholder="새 비밀번호" value={password} onChange={e=>setPassword(e.target.value)}/><button className="primary wide">변경하기</button></form><p>{msg}</p></main>}
