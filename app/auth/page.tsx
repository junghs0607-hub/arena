'use client';import {useState} from 'react';export default function Auth(){const [register,setRegister]=useState(false);const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch(`/api/auth/${register?'register':'login'}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});setMessage(r.ok?'환영합니다.':'인증 정보를 확인해주세요.')}return <main style={{maxWidth:420,margin:'100px auto',padding:32,background:'#fff',borderRadius:16}}><h1>{register?'회원가입':'로그인'}</h1><p>ClipForge AI 워크스페이스</p><form onSubmit={submit}><input required type="email" placeholder="이메일" value={email} onChange={e=>setEmail(e.target.value)}/><input required minLength={8} type="password" placeholder="비밀번호 (8자 이상)" value={password} onChange={e=>setPassword(e.target.value)}/><button className="primary wide">{register?'계정 만들기':'로그인'}</button></form><button className="link" onClick={()=>setRegister(!register)}>{register?'로그인으로 돌아가기':'처음이신가요? 회원가입'}</button><p>{message}</p></main>}
