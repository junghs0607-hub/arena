'use client';import {useState} from 'react';export default function Account(){const [currentPassword,setCurrent]=useState(''),[newPassword,setNew]=useState(''),[email,setEmail]=useState(''),[msg,setMsg]=useState('');async function change(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({currentPassword,newPassword})});setMsg(r.ok?'비밀번호가 변경되었습니다. 다시 로그인해주세요.':'현재 비밀번호를 확인해주세요.')}async function resend(){const r=await fetch('/api/auth/verification/resend',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});setMsg(r.ok?'인증 메일을 요청했습니다.':'요청에 실패했습니다.')}return <main style={{maxWidth:520,margin:'70px auto',padding:32,background:'#fff',borderRadius:16}}><h1>계정 보안</h1><p>ClipForge 계정과 인증 설정을 관리합니다.</p><h2>비밀번호 변경</h2><form onSubmit={change}><input required type="password" placeholder="현재 비밀번호" value={currentPassword} onChange={e=>setCurrent(e.target.value)}/><input required minLength={8} type="password" placeholder="새 비밀번호 (8자 이상)" value={newPassword} onChange={e=>setNew(e.target.value)}/><button className="primary wide">비밀번호 변경</button></form><h2>이메일 인증 메일 재전송</h2><div><input type="email" placeholder="가입 이메일" value={email} onChange={e=>setEmail(e.target.value)}/><button className="primary wide" onClick={resend}>인증 메일 보내기</button></div><p>{msg}</p></main>}
